angular.
  module('contact').
  component('contact', {
    templateUrl: 'contact/contact.html',
    controller: ['$routeParams',
      function ContactController($routeParams) {
        
      }
    ]
  });